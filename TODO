- Clean HTML by copying every style property to css/custom.css
- Change Bootstrap defaults.
- Change methods that retrieve data from model to call Spoonacular API
  (getAllDishes & getDish)

Home page:

Select dish:
    - Use this.update() function to display the elements.
    - Use bootstrap to display the dishes instead of manual aligning.
    - 

Dish details:

Dinner overview:

Dinner preparation:


